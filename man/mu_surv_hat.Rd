% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mu_surv_wrapper.R
\name{mu_surv_hat}
\alias{mu_surv_hat}
\title{\code{mu_surv_hat}: survivor baseline mean estimator \hat{\mu}_0(s; \beta)}
\usage{
mu_surv_hat(s, h, beta, X, Y, S, Z, tau)
}
\arguments{
\item{s}{evaluation landmark(s) (numeric scalar or vector)}

\item{h}{bandwidth}

\item{beta}{numeric vector of regression coefficients (length p)}

\item{X}{design matrix (p x n), columns aligned with Y/S/Z}

\item{Y}{length-n marker values (one per row/visit)}

\item{S}{length-n visit times (one per row/visit)}

\item{Z}{length-n event/censoring times replicated per row}

\item{tau}{survivor threshold \eqn{\tau}}
}
\value{
numeric vector of \eqn{\hat{\mu}_0(s;\beta)} with the same length as \code{s}
}
\description{
Kernel-smoothed estimator for the survivor/healthy group mean function \eqn{\mu_0(s)}.
This implements the discrete-sum ("blue") version on long-format data:
\deqn{
\hat{\mu}_0(s;\beta)=
\frac{\sum_{r=1}^n K_h(s-S_r)\, I(Z_r\ge\tau)\, Y_r}
{\sum_{r=1}^n K_h(s-S_r)\, I(Z_r\ge\tau)\, \exp\{\beta^\top X_r\}}
}
}
\details{
Each row \eqn{r} corresponds to one observed visit time \eqn{S_r = A_i + V_{il}}
in long-format data, with marker value \eqn{Y_r} and covariates \eqn{X_r}.
}
