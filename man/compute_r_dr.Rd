% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_r.R
\name{compute_r_dr}
\alias{compute_r_dr}
\title{Compute r(s, t; theta) and its derivatives with respect to theta}
\usage{
compute_r_dr(s, t, theta, sce, tau)
}
\arguments{
\item{s}{Numeric vector of s values.}

\item{t}{Numeric vector of t values (e.g., observed time Z).}

\item{theta}{Numeric vector of theta parameters.}

\item{sce}{Numeric scenario code (e.g., 1.1).}

\item{tau}{Numeric scaling constant used to normalize s and t inside
\eqn{\eta_\theta(s,t)} (this is \code{tau_norm}, not the survival cutpoint).}
}
\value{
A list with:
\itemize{
\item \code{r}: numeric vector of length \code{n}
\item \code{dr}: numeric matrix of dimension \verb{n x length(theta)}
}
}
\description{
Low-level utility used for gradient-based optimization and debugging.
Wraps the corresponding C++ implementation and returns both \code{r} and
the Jacobian matrix \code{dr}, where \verb{dr[i,k] = d r_i / d theta_k}.
}
\examples{
s <- seq(0, 10, length.out = 5)
t <- seq(2, 6, length.out = 5)
theta <- c(0.5, -1, 0.5)
out <- compute_r_dr(s, t, theta, sce = 1.1, tau = 20)
str(out)

}
